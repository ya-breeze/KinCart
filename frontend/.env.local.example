# Example environment configuration for local development
# Copy this file to .env.local and adjust as needed

# API URL - use this to connect to a locally running backend during development
# When empty or not set, the app will use relative paths (production default)
VITE_API_URL=http://localhost:8080
